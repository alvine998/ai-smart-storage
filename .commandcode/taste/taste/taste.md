# Taste
- For project audits, wants comprehensive multi-dimension coverage — explicitly asked for "performance, security, bugs, etc" (open-ended scope), and findings organized by severity with a prioritized fix order, not a single-focus review. Confidence: 0.6
- Expects audits to be evidence-based, not static reading alone: run `go build`/`go vet`/`go test`, check git history for committed secrets, and cite concrete file:line references in findings. Confidence: 0.5
- Follows up on audits by acting on the top-priority items immediately, and explicitly excludes specific findings with a reason when they'll handle them themselves/operationally (e.g., deferred the WhatsApp webhook-forgery fix because the secret will be configured after dev) — honor stated exclusions instead of implementing them anyway. Confidence: 0.6
- Gives terse, informal, low-detail briefs (lowercase, catch-all "etc") and delegates scoping/detail decisions to the agent; expects autonomous execution without follow-up questions mid-task. Confidence: 0.5
- project's API client (left open in the IDE); update it in sync with endpoint/auth changes so their testing setup keeps working. Confidence: 0.5
- Messaging-channel users (WhatsApp bot) must never require password login — expects phone-number-based identity with webhook signature auth only; password auth is reserved for the HTTP dashboard API. When touching auth middleware/routes, keep the public WhatsApp path credential-free. Confidence: 0.7
- Communicates in Indonesian (Bahasa Indonesia) — respond in Indonesian, keeping code, commands, and technical terms in English. Confidence: 0.7
- Security-conscious about stored data: proactively requested encrypting chat message content at rest ("messages.content should be encrypt for security reason"); when designing storage for sensitive user content, plan for encryption at rest (e.g., AES-256-GCM) rather than plaintext columns, with key management surfaced via env config. Confidence: 0.7
- When pasting broken behavior/error output from their bots with a terse "handle this" brief, expects an end-to-end autonomous fix: root-cause the code path, patch it, add regression tests covering the exact failing case, and verify with build/vet/test — not just an explanation of the symptom. Confidence: 0.6
- Prefers pure-logic unit tests over DB-integration-style testing: the repo's tests are pure-logic only with no sqlmock/DB mocking harness, and new command handlers (e.g. /checkfiles mirroring /balance) deliberately leave DB-dependent listing paths untested at the database level rather than introducing a mocking framework — keep the test surface as pure logic. Confidence: 0.7
- Terse one-line feature directives (e.g., "X should be encrypted for security") get the same end-to-end treatment as bug briefs: full implementation across code, config, .env.example, and README, with new unit tests, verified by build/vet/test — the agent scopes the details. Confidence: 0.6
- Values user guidance and discoverability in bot UX — explicitly frames new commands around helping users navigate the bot ("to guide user into this bot AI project"); the agent adds discoverability nudgets (e.g., /start mentioning /help) when aligned. Confidence: 0.7
